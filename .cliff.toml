[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
"""

body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper }}
    {% for commit in commits %}
        - {% if commit.breaking %}[BREAKING] {% endif %}{{ commit.message | upper_first }} \
            ([{{ commit.id | truncate(length=7, end="") }}]({{ commit.link | default(value="") }}))\
    {% endfor %}
{% endfor %}\n
"""

trim = true

footer = """
<!-- generated by git-cliff -->
"""

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false

commit_parsers = [
  { message = "^feat", group = "features" },
  { message = "^fix", group = "bug fixes" },
  { message = "^doc", group = "documentation" },
  { message = "^perf", group = "performance" },
  { message = "^refactor", group = "refactoring" },
  { message = "^test", group = "testing" },
  { message = "^chore\\(release\\)", skip = true },
  { message = "^chore", group = "miscellaneous tasks" },
  { message = "^ci", group = "continuous integration" },
  { message = "^ops", group = "operations" },
  { message = "^infra", group = "infrastructure" },
  { message = "^security", group = "security" },
]

protect_breaking_commits = false
filter_commits = false
tag_pattern = "v[0-9]*"
sort_commits = "oldest"

[bump]
auto = false
commit_message = "chore(release): prepare for {{new_version}}"
tag_message = "Radix HFT {{new_version}}"
commit_author = "radix-devops[bot] <devops@radix-hft.com>"
annotated_tag = true
